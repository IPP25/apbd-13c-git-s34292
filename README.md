## Merge Types Explanation

The merge of `feature-max` into `main` was **not fast-forward** because:
- After creating `feature-max`, an independent commit was made directly on `main` (e.g. README update).
- This caused the branches to diverge.
- Git therefore created a merge commit instead of just moving the pointer forward (fast-forward).
